// // array serve para armazenar varios dados em um unico lugar.

// // uma das formas de criar um array. primeiro cria, depois popula com as infos.
// int[] arrayInteiros = new int[5];
// arrayInteiros[0] = 10;
// arrayInteiros[1] = 20;
// arrayInteiros[2] = 30;
// arrayInteiros[3] = 40;
// arrayInteiros[4] = 50;

// // para exibir os valores, é necessário um laço de repetição.
// foreach (int valor in arrayInteiros)
// {
//   Console.WriteLine(valor);
// }

// // é possivel popular o array durante a sua criação, para isso é preciso indicar a quantidade de elementos que terão no array.
// string[] arrayString = new string[2] { "matheus", "bastos" };

// for (int i = 0; i < arrayString.Length; i++)
// {
//   Console.WriteLine(arrayString[i]);
// }